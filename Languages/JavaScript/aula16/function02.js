function soma(n1=0, n2=0) { // O " =0 " após o parâmetro é um parâmetro adicional, passado caso não haja o recebimento do parâmetro
    return (n1 + n2)
}

console.log(soma(7, 2))
// console.log(soma(7)) // passei apenas um parâmetro para utilizar o parametro opcional